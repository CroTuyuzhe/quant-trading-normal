---
name: quant-trading
description: |
  量化交易助手 — A股量化分析、交易决策与报告记录的统一入口。当用户发送"量化交易"或"/quant"时触发。
  核心依托 cn-stock-quant（个股量化分析）和 quant-stock-screener（多因子选股）两个已安装 Skill。
  覆盖三大能力：(1) 量化分析记录 — 自动留存分析历史到专属记录文件；(2) 量化交易执行 — 引导式交互流程，输出买入/观望/规避结论；(3) 量化报告生成 — 每次执行后自动生成可视化报告并归档。
  注意：本 Skill 的触发词为"量化交易"和"/quant"，其他分析类指令（如"分析600900"、"选股"）仍由各自原始 Skill 处理。
---

# 量化交易助手

统一调度 cn-stock-quant 与 quant-stock-screener，聚焦量化分析→决策→记录闭环。

## 执行流程（严格单流程，不自动循环）

### 阶段一：引导输入

触发后立即返回引导话术：

```
📊 量化交易助手已启动

请输入要分析的个股代码（留空将自动执行全市场选股）：
```

用户输入代码后，继续引导：

```
请输入量化执行步数（如 15，即重复 15 次因子分析并汇总结论；留空则单次分析，直接给出交易结论）：
```

### 阶段二：量化执行

根据用户输入分两条路径：

**路径 A — 个股分析（用户输入了股票代码）：**

对每一步（step = 1..N）：

1. 调用 cn-stock-quant 核心分析：
   ```bash
   cd {baseDir}/../cn-stock-quant/scripts && python3 quant_analysis.py <CODE> --format json
   ```
2. 解析 JSON 输出，提取关键因子信号：估值分位、因子评分、风险指标、动量信号
3. 根据因子权重生成单步交易结论（买入/观望/规避）
4. **非最后一步：** 仅返回极简一行：`HH:mm — 结论（买入/观望/规避）`，然后等待约 60 秒再执行下一步
5. **最后一步：** 输出完整量化分析报告（见阶段四），并在报告末尾附上所有步的运行汇总表

**多步执行示例（3步）：**
```
开始 600104 量化分析 x3...

20:06 — 观望
20:07 — 观望
20:08 — 观望

📋 量化交易报告 — 2026-04-02 20:08
[完整报告...]
```

**路径 B — 全市场选股（用户留空股票代码）：**

1. 调用 quant-stock-screener 进行选股：
   ```bash
   cd {baseDir}/../quant-stock-screener/scripts && python3 screener_main.py --strategies 123456 --market a --top 30 --format json
   ```
2. 取 Top 5 个股，逐只调用 cn-stock-quant 深度分析
3. 为每只输出独立交易结论

### 阶段三：决策权重规则

最终交易结论基于以下因子加权：

| 维度 | 权重 | 判定逻辑 |
|------|------|----------|
| 估值分位 | 25% | PE/PB 分位 < 30% → 偏买入 |
| 因子信号 | 30% | 9因子综合评分 > 1.5 → 偏买入 |
| 风险指标 | 20% | 夏普 > 1 且最大回撤 < 15% → 偏买入 |
| 动量信号 | 15% | 动量分组回测收益 > 0 → 偏买入 |
| 资金面 | 10% | OBV 趋势向上且筹码集中 → 偏买入 |

**综合阈值：**
- 加权得分 > 0.6 → ✅ **建议买入**
- 加权得分 0.3~0.6 → ⏸️ **建议观望**
- 加权得分 < 0.3 → ⛔ **建议规避**

### 阶段四：报告生成与归档

每次执行完毕后：

1. **生成报告** — 汇总本次所有分析数据：
   ```
   📋 量化交易报告 — YYYY-MM-DD HH:mm

   一、执行概要
      股票代码 / 选股模式
      执行步数: N
      最终结论: 买入/观望/规避

   二、因子分析详情
      各因子得分、分位数、信号强度

   三、每步运行汇总（多步时）
      HH:mm — 结论
      HH:mm — 结论
      HH:mm — 结论
      最终判定: X步买入/ X步观望/ X步规避

   四、综合评分
      加权得分: xxx (阈值判定)

   五、风险提示
      以上分析仅供参考，不构成投资建议。
   ```

2. **写入记录文件** — 追加到分析记录文件：
   ```bash
   records_dir = {baseDir}/references/records/
   record_file = records_dir/YYYY-MM-DD.json
   ```
   每条记录包含：时间戳、股票代码/选股策略、执行步数、各步因子数据、最终结论。

3. **展示报告** — 将报告以结构化消息返回用户。

## 记录文件结构

```json
{
  "date": "2026-04-02",
  "records": [
    {
      "timestamp": "2026-04-02T20:30:00+08:00",
      "mode": "stock",
      "code": "600900",
      "steps": 5,
      "factors": {
        "valuation_percentile": 0.25,
        "factor_score": 1.8,
        "sharpe_ratio": 1.2,
        "max_drawdown": -0.12,
        "momentum_return": 0.08,
        "obv_trend": "up"
      },
      "weighted_score": 0.72,
      "conclusion": "买入",
      "step_results": [
        {"step": 1, "score": 0.7, "conclusion": "买入"},
        {"step": 2, "score": 0.75, "conclusion": "买入"}
      ]
    }
  ]
}
```

## 防卡死规则

1. **单流程执行** — 每次用户触发"量化交易"只执行一轮完整流程（引导→输入→分析→报告），不自动重复
2. **步数上限** — 量化执行步数上限为 20，超出自动截断
3. **步间间隔** — 多步执行时，每步间等待约 60 秒（非最后一步返回极简结论后等待）
4. **超时兜底** — 单只股票分析调用 timeout 120s，选股调用 timeout 300s
5. **错误处理** — 调用子 Skill 失败时记录错误并给出部分结论，不无限重试
6. **不追问** — 执行完毕后输出报告即结束，等待用户下次触发，不主动追问

## 查询历史记录

用户说"查看量化记录"或"量化历史"时，读取 records 目录下对应日期的 JSON 文件并展示摘要。
